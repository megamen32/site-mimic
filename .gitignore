# local run artifacts
examples/*/ch.json
__pycache__/
*.pyc
*.test
.fast-agent/
.tmp/

# compiled daemon binary — build locally: go build -o fetchd ./cmd/fetchd
/fetchd

# agent runtime state (task records under .agents/tasks/ stay tracked)
.agents/shared-session/
.worktrees/
